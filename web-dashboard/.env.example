# Mọi lời gọi đi qua gateway (cổng 8080), không gọi thẳng java-core hay ai-service
VITE_GATEWAY_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
VITE_APP_NAME=CRM AI

# Bật tầng mock MSW thay cho backend thật. Đặt false để gọi gateway :8080.
VITE_USE_MOCK=true
