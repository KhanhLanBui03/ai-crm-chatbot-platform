# ai-service — Track B. Python 3.11.
# Chưa ghim phiên bản chính xác ở bước dựng khung; ghim khi bắt đầu code (Sprint 1)
# và ghi vào báo cáo để đảm bảo tính tái lập (kế hoạch mục 8.1).

# --- Web ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
python-dotenv>=1.0

# --- Điều phối (orchestrator) ---
langgraph>=0.2.50
langchain-core>=0.3
langchain>=0.3

# --- Dữ liệu: pgvector trong cùng cụm PostgreSQL (ADR-0007) ---
psycopg[binary,pool]>=3.2
pgvector>=0.3.6
sqlalchemy>=2.0
alembic>=1.14

# --- Kafka (kế hoạch mục 4.3) ---
# Chọn MỘT trong hai. aiokafka hợp với FastAPI async hơn; confluent-kafka nhanh hơn.
aiokafka>=0.12
# confluent-kafka>=2.6

# --- Service discovery: FastAPI không tự đăng ký Eureka (kế hoạch mục 4.4) ---
py-eureka-client>=0.11

# --- RAG: trích xuất và chuẩn hóa (mục 7.1 chặng 1-2) ---
pymupdf>=1.24
python-docx>=1.1

# --- RAG: tách từ tiếng Việt cho BM25 (chặng 4) ---
pyvi>=0.1.1
underthesea>=6.8

# --- RAG: nhúng và xếp hạng lại (chặng 5, 9) ---
sentence-transformers>=3.3
FlagEmbedding>=1.3
torch>=2.5

# --- Chấm điểm Lead và phân nhóm chủ đề ---
scikit-learn>=1.6
pandas>=2.2
numpy>=2.1

# --- Khung đánh giá (mục 8) ---
# Dùng thư viện có sẵn cho Recall/MRR/nDCG thay vì tự cài đặt công thức (mục 1.2)
ranx>=0.3.20
pyyaml>=6.0
jinja2>=3.1          # sinh báo cáo HTML
matplotlib>=3.9      # biểu đồ cho báo cáo

# --- Quan sát hệ thống (mục 4.5) ---
prometheus-client>=0.21
python-json-logger>=2.0

# --- Kiểm thử ---
pytest>=8.3
pytest-asyncio>=0.24
