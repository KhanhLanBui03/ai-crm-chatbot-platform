# Model Context Protocol (MCP) Tool Service

Provides standardized interfaces for tools that the LLM agent can call (e.g. check stock, check price, create lead, search products).
Exposed on port `8082`.
