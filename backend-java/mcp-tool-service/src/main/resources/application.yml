spring:
  application:
    name: mcp-tool-service

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

main-backend:
  url: ${MAIN_BACKEND_SERVICE_URL:http://localhost:8081}
