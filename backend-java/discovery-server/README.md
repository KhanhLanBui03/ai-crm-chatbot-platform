# Discovery Server

Netflix Eureka registration and discovery server.

## Run Locally
1. Run `./mvnw spring-boot:run` or compile and run the JAR.
2. The Eureka dashboard will be available at `http://localhost:8761`.
